<h1>People</h1>
<table class="table table-striped">
  <tr>
    <th>Name</th>
    <th>NIN</th>
  </tr>
  <%@people.each do |person|%>
    <tr>
      <td><%=link_to person.name, "#"%></td>
      <td><%=person.nin%></td>
    </tr>
  <%end%>
</table>
<%=link_to "Add a person", new_person_path, class: "btn btn-primary"%>
